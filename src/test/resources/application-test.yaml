platform: mysql # h2, postgres, mysql, mariadb, oracle, sqlserver, hana, clickhouse, sqlite
ddlMode: migration # none | dropCreate | migration
dbName: chatserver

ebean:
  ddl:
    generate: false
    run: false